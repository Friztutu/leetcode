#include "string.h"

char* gcdOfStrings(char* str1, char* str2);

int main(void) {
  return 0;
}

char* gcdOfStrings(char* str1, char* str2) {
  char* resultString = malloc(sizeof(char) * strlen(str1));

  
}